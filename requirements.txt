# Core ML/AI - Version locked for compatibility
openai>=1.40,<2.0.0
langchain>=0.3.0
langchain-openai>=0.3.0
langchain-community>=0.3.0
langgraph>=0.6.0
langsmith>=0.4.0

# Supabase (Database + Vector Store)
supabase>=2.0.0
pydantic>=2.0,<3.0
postgrest-py>=0.10.0
httpx>=0.26,<0.29

# Data Processing
python-dotenv>=1.0.0

# External Services
resend>=0.7.0
twilio>=8.0.0

# Note: Removed streamlit, matplotlib, pandas, numpy, psycopg2, sqlalchemy
# These are not needed for Vercel serverless API functions
# Install locally with: pip install streamlit matplotlib pandas numpy
