# Core ML/AI - Pinned versions for fast resolution
openai==1.54.0
langchain==0.3.7
langchain-openai==0.2.8
langgraph==0.2.34
langsmith==0.1.139

# Supabase (Database + Vector Store)
supabase==2.9.0
pydantic==2.9.2
postgrest-py==0.10.6
httpx==0.27.2

# Config
python-dotenv==1.0.1

# External Services (optional - graceful degradation)
resend==2.4.0
twilio==9.3.6

# Minimal dependencies for fast Vercel builds
# Install dev dependencies locally: pip install streamlit matplotlib pandas
