# Minimal core dependencies - let pip auto-resolve transitive deps
langgraph>=0.2.0
langchain-openai>=0.2.0
supabase>=2.0.0
python-dotenv>=1.0.0
resend>=2.0.0
twilio>=9.0.0

# Note: langchain, openai, httpx, pydantic will be installed as dependencies
# This avoids version conflicts by letting pip resolve the dependency tree
